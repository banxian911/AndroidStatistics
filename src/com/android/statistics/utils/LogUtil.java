package com.android.statistics.utils;


public class LogUtil {
    public static boolean flag = HelpUtil.canShowLog() ? true : false;  
    public static boolean error_flag = HelpUtil.canShowLog() ? true : false; 
    public static boolean is_jiami = HelpUtil.canShowLog() ? false : true; 
}
